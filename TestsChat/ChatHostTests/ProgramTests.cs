using System;
using System.Collections.Generic;
using System.Text;
using Xunit;
using Moq;
using WCF_Chat;
using System.ServiceModel;

namespace TestsChat.ChatHostTests
{
    /* public class ProgramTests
     {
         ServiceHost host = new ServiceHost(typeof(ServiceChat));
         [Fact]
         public void OpenHost_HostIsOpened()
         {
             //var mockhost = new Mock<ServiceHost(typeof(WCF_Chat.ServiceChat))>();
             try
             {
                 host.Open();

                 var stateHost = (int)host.State;
             }
             catch
             {
                 Assert.Throws<TimeoutException>(host.Open);
             }

         }


     }
    */
}
